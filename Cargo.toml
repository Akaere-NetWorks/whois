[package]
name = "whois"
version = "0.1.2"
edition = "2021"
description = "A simple WHOIS query tool"
authors = ["Pysio"]
license = "AGPL-3.0"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
anyhow = "1.0"
colored = "2.0"

[profile.release]
lto = true
opt-level = "z"
strip = true